apiVersion: v1
kind: Pod
metadata: 
  name: postgres-pod
  labels:
    name: postgres-pod 
    app: example-voting-app
spec:
  containers:
    - name: postgres
      image: postgres
      port:
        - containerPort: 5432
      env:
        - name: POSTGRES_ROOT_PASSWORD
          value: mysecretpassword
